# Homework 1 — Files, paths, terminal, and run instructions

**ECBS5293 — Computing for Analytical Work · due the Friday after Session 1, 23:59 (slot on Moodle) · 5–6 hours**

## Goal

A colleague handed this project over. They need `output/report.csv` regenerated from the supplied data — and proof that it was. The project is supposed to look like this:

```text
data/raw/sales_2024.csv     the input data (committed)
notebooks/explore.ipynb     loads the sales data and prints a per-product unit count
scripts/export.py           writes output/report.csv with revenue per product
output/report.csv           generated by the script; not committed
```

Both parts should work from a fresh clone:

1. `notebooks/explore.ipynb` — loads the sales data and prints a per-product unit count.
2. `scripts/export.py` — writes `output/report.csv` with revenue per product.

## Get the project

In your terminal (Git Bash on Windows, Terminal on macOS), in the folder where you keep course work:

```bash
git clone https://github.com/earino/ecbs5293-hw01-files-paths-terminal.git
cd ecbs5293-hw01-files-paths-terminal
```

## Reproduce the failure (as currently documented)

These are the run instructions the previous owner left. They are part of the problem. In the terminal, from the project folder, follow them exactly and read what happens; the comment line is done in VS Code.

```bash
uv sync
# open this folder in VS Code, open notebooks/explore.ipynb, select the .venv kernel
cd scripts
uv run python export.py
```

## What is broken

Nothing runs as documented, and there is more than one thing wrong. Work through it one failure at a time: symptom → inspect → cause → smallest fix → verify. Before you call anything done, check it against the deliverables below. Some of it is fixed in the terminal; some needs a file opened and edited in VS Code (open this folder, then the file).

## What you must submit (all on Moodle)

In the project folder:

1. The repaired project: notebook passes *Restart Kernel and Run All*; `uv run python scripts/export.py` from the project folder writes `output/report.csv` *inside the project*.
2. **`README.md`** rewritten with correct, minimal run instructions a classmate could follow from a fresh unzip: the exact commands that worked, in order, from the project folder. This is your command log.
3. **`DIAGNOSIS.md`** — one five-part note per failure you fixed, raw output pasted in part 3. Organise the notes however makes the causes clearest; what is graded is that every cause and its evidence appears. Part 5 of the export's note carries the verification, in two layers:
   - **Sanity checks** — data rows in the input (`wc -l`, minus the header line), rows the notebook loaded (`df.shape`), products in the report. These catch a file that was never read. They do **not** catch wrong numbers: a report with every revenue multiplied by 100 still lists four products.
   - **Reconciliation** — two totals computed by *different code* that must agree to the cent. In the notebook, total revenue from the input: `(df["units"] * df["unit_price"]).sum()`. Then the total of the report the script wrote: `pd.read_csv(<the report, resolved from the project root>)["revenue"].sum()`. The last cell of the notebook is a scaffold for exactly this. State both numbers. If they differ, the report did not come from this data as it stands, and you are not done. If they agree, say exactly what that shows: the report's **aggregate** revenue matches the data. It does not by itself establish every product's value (four wrong values that add up to the same total would pass), so claim no more than you checked.
4. **`AI_USE.md`** — a few sentences: what you asked an assistant (if anything), what it got right or wrong, how you verified. "Did not use AI" is a fine answer.

Then make the archive — one command, from the project folder:

```bash
uv run python make_submission.py
```

It writes `hw1-submission.zip` in the folder *above* the project and leaves out `.venv/`, `output/`, and other generated files. Open the zip and look: your code, data, notes, and README — nothing else.

Upload to the Homework 1 slot on **Moodle**:

5. `hw1-submission.zip` (from the folder above the project)
6. A **60–90 second screen-recorded video** with your voice. Start by saying "Homework 1" and the repo name. Run the fixed code, then, for **every failure you fixed**, say what broke, why, what you changed, and how you verified it. Those four elements are what the video is scored on, and a failure the video skips costs you on the cause and change elements. No script-reading; face not required; MP4 under 50 MB.

## Rules

- No paths to your own machine. The grader unzips your archive and runs your instructions.
- `git status` and `git diff` are how you see what you changed — paste **both** into part 4 of your note. `git diff` shows edits to files Git already tracks; a file you *moved* appears in the diff only as its deletion at the old place — the new location is `untracked`, which `git status` shows and the diff does not — so say the move in words as well. That is all the Git this homework needs.
- Do not zip by hand; the helper leaves out `.venv/` and `output/` for you. If you do it yourself, you are responsible for what is in the archive.
- Run instructions may include preparation steps — a folder to create, a file to move — and the grader runs them in order. A script that does its own preparation is kinder to the next person; it is not required.
- AI may explain errors and suggest what to inspect. You must be able to explain every change in the video.

## Hints, if stuck

1. In the notebook: `import os; os.getcwd()`. In the terminal: `pwd`, `ls`, `ls data`.
2. `find . -name "*.csv"` (macOS/Git Bash) lists every CSV under the project. Is the data where the code thinks it is?
3. The script prints where it wrote. Read that line as a full path, then `ls` that folder. Is that where the deliverables say the report belongs?

## Grading

See the rubric on the course site. The diagnosis note and the video together outweigh the fix.

## If you got lost: how to reset

Both of these **destroy work**. Read before running.

**Discard uncommitted changes (destructive)** — throw away edits and new files; keep your commits:

```bash
git restore --staged --worktree .    # every tracked file back to the last commit, staged or not
git clean -fd                        # and remove new, untracked files
```

> ⚠️ Permanently deletes uncommitted changes — staged or not — and any new untracked files.

**Full reset to the starter state (destructive)** — back to exactly what you cloned; throws away your commits too:

```bash
git reset --hard origin/main
git clean -fdx
```

> ⚠️ Discards your local commits and uncommitted changes. The `-x` also removes ignored files — `output/`, the `.venv/` environment — so the folder truly matches a fresh clone (`uv sync` rebuilds the environment in a minute). Without `-x`, leftover generated files can hide the very failure the lab wants you to meet again.
